import numpy as np
from matplotlib import pyplot as plt

from models import perceptron_fixed_data, adaline_fixed_data, perceptron_generated_iterator

perceptron_fixed_data(10)
adaline_fixed_data(10)

